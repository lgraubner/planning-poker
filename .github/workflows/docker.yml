name: Docker

# The image build is the slowest check and only breaks when its inputs change,
# so PRs run it only then. Every push to main still verifies the deploy artifact.
on:
  push:
    branches: [main]
  pull_request:
    paths:
      - Dockerfile
      - .dockerignore
      - go.mod
      - go.sum
      - web/package.json
      - web/package-lock.json
      - scripts/container-smoke.sh
      - .github/workflows/docker.yml

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  smoke:
    name: Container smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: docker/setup-buildx-action@v4
      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: planning-poker:smoke
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Run smoke test
        run: sh scripts/container-smoke.sh
